<%= form_for @idea do |f| %>
<%= f.text_field :title, placeholder: "Idea's name" %></br>
<%= f.text_area :description, placeholder: "What is your idea about?" %></br>
<%= f.collection_select :category_id, @categories, :id, :title %></br>
<%= f.submit %>
<% end %>
